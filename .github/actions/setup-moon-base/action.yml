name: Ensure moon default branch
description: Fetch a local main ref so moon can resolve vcs.defaultBranch

runs:
  using: composite
  steps:
    - name: Fetch main
      shell: bash
      run: git fetch --no-tags origin main:main
