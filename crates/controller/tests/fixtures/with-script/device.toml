schema_version = 1
id = "test.script"
vendor_name = "Script"
product_name = "Test"
midi_name_contains = ["Script"]

[toml-schema]
version = "1.0.0"
location = "../../../../../schemas/device.tosd"

[deck_1]
play_pause = { type = "note", channel = 1, note = 0x0B }
