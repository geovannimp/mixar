# Vendored [Rubber Band Library](https://breakfastquay.com/rubberband/) (GPL-2.0+)
# for realtime key lock when system `librubberband` is unavailable.
# Prefer pkg-config (`librubberband-dev`) when present; see crate `build.rs`.
