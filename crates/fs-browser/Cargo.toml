[package]
name = "fs-browser"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Filesystem volume listing and directory browsing for audio file pickers"

[dependencies]
codec = { path = "../codec" }
serde = { workspace = true, features = ["derive"] }

[dev-dependencies]
tempfile = "3"
