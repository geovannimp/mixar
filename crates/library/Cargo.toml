[package]
name = "library"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "Library manager for rust-dj-engine"

[dependencies]
library-core = { path = "../library-core" }
audio-core = { path = "../audio-core" }
lofty = "0.22"
log.workspace = true
serde = { workspace = true, features = ["derive"] }
serde_json = "1.0"
zstd = "0.13"
sea-orm-sync = { version = "2.0.0", features = [
    "rusqlite",
    "entity-registry",
    "schema-sync",
] }
rusqlite = { version = "0.38", features = ["bundled"] }
analyzer = { path = "../analyzer", optional = true }
analyzer-core = { path = "../analyzer-core", optional = true }

[features]
default = ["analysis"]
analysis = ["dep:analyzer", "dep:analyzer-core"]

[dev-dependencies]
tempfile = "3.0"
