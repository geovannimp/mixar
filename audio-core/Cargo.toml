[package]
name = "audio-core"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "Core audio traits and types for rust-dj-engine"

[dependencies]
anyhow.workspace = true
thiserror.workspace = true
serde.workspace = true
dasp.workspace = true

# Built so hooks-install's build.rs can install betterhook on first local build.
[build-dependencies]
hooks-install = { path = "../hooks-install" }
